.row
  .col
    %h1= t('users.edit_title')
    = form_for @user do |f|
      .field
        = f.label :role
        = f.select :role, User.roles.keys.map {|role| [t("users.roles.#{role}"), role] }
      = f.submit t('actions.save')