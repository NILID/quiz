.row
  .col.col-12
    - if @themes.any?
      %ul.tags
        - @themes.each do |theme|
          %li
            = link_to theme do
              = theme.title
              %span= theme.questions_count
    - else
      .empty-block
        = t('titles.empty')
